// Displays the page title banner used across the storefront pages.
function WelcomeBand() {
    return (
        <div className="welcome-band">
            <h1 className="welcome-band__title">Book List</h1>
        </div>
    );
}

export default WelcomeBand;
